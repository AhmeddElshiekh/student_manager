<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- This permission is kept as it's used for the 'call parent' feature. -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <!--
    The MANAGE_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE, and READ_EXTERNAL_STORAGE
    permissions have been removed. We now use the Storage Access Framework (via file_picker)
    which is the Google-recommended best practice and does not require these permissions.
    This resolves the Google Play policy violation.
    -->

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

    <application
        android:label="Nizam"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        
        <!-- Fix for 16KB page size compatibility on newer devices. -->
        <uses-native-library android:name="libflutter.so" android:required="false" />

        <!--
        The `requestLegacyExternalStorage` attribute has been removed as it is no longer needed
        and is related to the deprecated storage access model.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

</manifest>
